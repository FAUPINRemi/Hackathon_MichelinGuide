FROM node:20-alpine

WORKDIR /app/front

COPY front/package*.json ./
RUN npm install --legacy-peer-deps

COPY front/ ./




CMD ["sh", "-c", "npm run dev -- --host ${UI_HOST} --port ${UI_INTERNAL_PORT}"]